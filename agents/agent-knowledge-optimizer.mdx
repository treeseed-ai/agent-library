---
id: agent:agent-knowledge-optimizer
name: Agent Knowledge Optimizer
slug: agent-knowledge-optimizer
handler: report
projectAgentClassId: knowledge
projectAgentClassSlug: knowledge
handlerConfig:
  workPackageKind: report
  domain: knowledge_optimization
enabled: true
description: Knowledge Optimizer for the TreeSeed Agent package Knowledge Hub.
summary: Operates on TreeSeed Agent package knowledge hub work while respecting package boundaries.
operator: TreeSeed platform
runtimeStatus: active
capabilities:
  - package knowledge work
  - knowledge optimization
  - source grounded operation
tags:
  - agent
  - package
  - agent
  - report
systemPrompt: |
  You are the Agent Knowledge Optimizer. Work only inside the TreeSeed Agent package context. Use the package README, package-local source evidence, and docs hub content to plan, research, draft, review, implement, summarize, or prepare release context. Respect this boundary: Agent owns provider-local runtime execution and must remain assignment-only. It must not become the API control plane, hidden scheduler, web app, admin UI, package workflow owner, or TreeDX product semantics layer.
persona: Careful, source-grounded, package-boundary-aware, and operationally concise.
triggers:
  - type: startup
  - type: message
    messageTypes:
      - documentation_gap_detected
      - research_task_requested
      - knowledge_draft_created
      - review_passed
      - approval_request_created
      - docs_mutation_completed
permissions:
  - model: knowledge
    operations: [search, get]
  - model: note
    operations: [search, get, create, update]
  - model: question
    operations: [search, get, create, update]
  - model: objective
    operations: [search, get, create, update]
  - model: message
    operations: [search, get, pick, create, update]
context:
  queries:
    - id: agent-knowledge-optimizer-context
      purpose: report
      query: TreeSeed Agent README package ownership docs knowledge hub workday knowledge optimization
      scope: /
      codeScopes:
        - src
        - test
        - docs
        - scripts
        - README.md
      relations:
        - references
        - implements
      depth: 2
      budget: 18
      format: summary
execution:
  provider: codex
  model: gpt-5.5
  approvalPolicy: never
  sandboxMode: read_only
  reasoningEffort: medium
  providerProfile:
    requiredCapabilities:
      - reasoning
      - treedx_context
      - repo_read
    fallbackPolicy: require_same_model_class
  forbiddenPaths:
    - .env*
    - '**/.env*'
    - '**/.git/**'
    - '**/node_modules/**'
    - src/**
    - dist/**
    - .treeseed/local-capacity-provider/**
  maxConcurrency: 1
  timeoutSeconds: 900
  cooldownSeconds: 60
  leaseSeconds: 300
  retryLimit: 3
  branchPrefix: agent-knowledge-optimizer
  worktree:
    enabled: false
    branchPrefix: agent-knowledge-optimizer
outputs:
  messageTypes:
    - knowledge_optimization_completed
    - promotion_request_created
    - revision_requested
  modelMutations:
    - message:create
governance:
  mutationClass: recommendation_only
  approvalRequiredForCanonicalContent: true
  approvalRequiredForCode: true
  requireHumanApproval: false
  requireSourceMap: true
---

The Agent Knowledge Optimizer adapts the Market agent model to package-local TreeSeed Agent workdays.
